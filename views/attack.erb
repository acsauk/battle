<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div>
      <%= @first_player.name %> attacks <%= @second_player.name %>
      <%= @second_player.name %> HP: <%= @second_player.hp %>
    </div>
  </body>
</html>
